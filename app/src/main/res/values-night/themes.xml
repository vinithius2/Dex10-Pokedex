<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal escuro -->
    <style name="Theme.Dex10" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/red_200</item>
        <item name="colorPrimaryVariant">@color/red_700</item>
        <item name="colorOnPrimary">@color/black</item>

        <item name="colorSecondary">@color/yellow_200</item>
        <item name="colorSecondaryVariant">@color/yellow_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    </style>

    <!-- Splash escura (opcionalmente pode mudar o fundo, mas pode deixar igual ao claro) -->
    <style name="Theme.Dex10.Splash" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@drawable/splash_layer_list</item>
        <item name="windowSplashScreenBackground">@color/red_700</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/red_200</item>
        <item name="postSplashScreenTheme">@style/Theme.Dex10</item>
        <item name="android:icon">@mipmap/ic_launcher</item> <!-- Isso define o ícone -->
    </style>

</resources>
